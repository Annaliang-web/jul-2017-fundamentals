// good morning fundamentals!

// javascript
// data
// persistance - it stays around
4 + 2; // that does not persist
let result = 4 + 2;  // container that holds information or data, now this data persists

console.log(result);
let result2 = 5 * 5;

console.log(result + result2);

// loops
// we want the machine to do the hard work
// adobe flash - premiere animation tool
// tweening
// keyframing
let keyframe = 0;

// what loop do you want to use
// what condition needs to be met for us to execute the code block
// a code block is represented by {}
while (keyframe < 10) {
  console.log(keyframe);
  // keyframe = keyframe + 1;
  // keyframe += 1;
  keyframe++; // this keyframe variable changes everytime the block of code is executed
}

// for loop
// for (init; condition; increment) {
for (let keyframe = 0; keyframe < 10; keyframe++) {
  console.log(keyframe);
}

// html css
//  tim berners lee
// cern labratories

// <html>
// </html>
//
// html == meaning

// css - inline styling
// <h1 style="color: pink">

// css == styling








//
